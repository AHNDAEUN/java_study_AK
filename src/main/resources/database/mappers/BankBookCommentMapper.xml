<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.iu.home.bankBook.BankBookCommentDAO">

	
	<update id="setCommentUpdate" parameterType="BankBookCommentDTO">
	
	UPDATE BBCOMMENT SET CONTENTS =#{contents} WHERE NUM= #{num}
	</update>
	
  	<delete id="setCommentDelete" parameterType="BankBookCommentDTO">
	DELETE BBCOMMENT WHERE num = #{num}
</delete> 
	
	
	<select id="getCommentListTotalCount" parameterType="com.iu.home.util.CommentPager" resultType="java.lang.Long">
		SELECT COUNT(NUM) FROM BBCOMMENT WHERE BOOKNUM = #{bookNum}
	</select>
	
	
	<select id="getCommentList" parameterType="CommentPager" resultType="BankBookCommentDTO">
		SELECT * FROM
		(SELECT ROWNUM R,C.* FROM
		(SELECT * FROM BBCOMMENT
		WHERE BOOKNUM= #{bookNum}
		ORDER BY NUM DESC) C)
		WHERE R BETWEEN #{startRow} AND #{lastRow}
	</select>
	
	<insert id="setCommnetAdd" parameterType="BankBookCommentDTO">
		INSERT INTO BBCOMMENT(NUM, BOOKNUM, WRITER, CONTENTS, REGDATE)
		VALUES (BOARD_SEQ.NEXTVAL, #{bookNum}, #{writer}, #{contents}, sysdate)
	</insert>
	</mapper>

